  {% extends "header.html" %}
  {% block title %}{{ title }}{% endblock %}
	{% block content %}
	<div class="jumbotron">
    <h1>{{ header }}</h1>
    <hr/>
    <h2>{{ record.title }}</h2>
    <p>{{ record.message }}</p>
    <strong>{{ record.pub_date }}</strong>
    <p>
      <a class="btn btn-lg btn-primary" href="{% url 'landing' %}" role="button">back</a>
    </p>
    <hr/>
  </div>
	{% endblock %}

